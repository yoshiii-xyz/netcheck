[package]
name = "netcheck"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Check network connectivity and DNS resolution"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
anyhow = "1.0"
